{
  "name": "mergeworks-functions",
  "description": "Cloud Functions for Mergeworks",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint --ext .ts src"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "eslint": "^9.13.0",
    "eslint-config-google": "^0.14.0",
    "@types/node": "^20.12.12"
  },
  "private": true
}
